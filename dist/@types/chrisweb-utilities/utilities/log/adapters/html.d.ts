/**
 *
 * html log
 * create a div an insert the messages in div
 * can be usefull on mobile if no other console is available
 *
 */
declare const htmlLog: (logObjects: any, logObjectsLength: number, logFontColor: string, logBackgroundColor: string) => void;
export { htmlLog };
